#include "holberton.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * strtow - check the code for Holberton School students.
 * @str:string origin
 * Return:a pointer my_str
 */

char **strtow(char *str)
{
char **string;
int countletters = 0, int words = 0  int count = 0;

words = count(str);

string = malloc(sizeof(char *) * words);
if (string == NULL)
return (NULL);
str = malloc







}





