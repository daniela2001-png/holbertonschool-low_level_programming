#include "holberton.h"
/**
 * main - Entry point
 * @argc : count
 * @argv : argument vector
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	(void)argv;
	return (0);
}
