#include "holberton.h"
#include <stdio.h>
/**
 * _isdigit - check the code for Holberton School students.
 * @c: The character to print
 * Return: Always 0.
 */

int _isdigit(int c)
{
	if (c == a && c== z)
		{
			return (0);
		}
	else
		{
			return (1);
		}
}
